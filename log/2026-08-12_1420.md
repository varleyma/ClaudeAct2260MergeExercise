# Log — 2026-08-12 14:20 — Experian credit-bureau outmigration + zip-level treatment

## New data source
`data\raw\header2005_2023_ctk.dta` in Dropbox (READ-ONLY, 2GB Stata): Experian
credit header panel, person(ctk)×year 2005–2023, 56.6M rows / ~2.9M consumers
per year nationally. Variables: ctk, record_nb, m_key, year, zip_cd (5-digit,
clean), vantage_v4_score. Sorted by year; no duplicate ctk-year pairs.

## Headline answers
- **237 distinct PR zipcodes** (prefixes 006/007/009; 008=USVI) appear in the data.
- 442,722 PR person-year rows; 49,212 consumers ever observed in a PR zip;
  ~21–27K PR consumers per year (declining over time, consistent with population).
- Outmigration rates (t→t+1, consumers observed both years): move-any-zip
  ~12% (2005) declining to ~7% (2019+); off-island component 2–3.7%/yr with the
  max in 2017 (post-María 2017→2018 moves) — good external validation.
- Attrition (in PR at t, unobserved t+1): ~28K person-years, kept separate from moves.

## Scripts (code/python/)
- `inspect_experian_header.py` — structure peek without full load
- `experian_pr_scan.py` — pass 1 chunked scan (68s): PR zip universe, year counts, PR ctk set
- `experian_pr_migration.py` — pass 2 (~1.5 min): extracts 523,835 history rows for PR-ever
  ctks → transitions. Definitions: risk set = in PR zip at t & observed t+1;
  move_any (different zip incl. within-PR), move_within_pr, move_off_pr; attrition separate.
- `build_zip_treatment.py` — zip-level treatment mirroring design2 tract DiD:
  spatial join of 1,256 design1 events (lon/lat) to 2020 ZCTA polygons
  (nearest-fallback for 5 coastal points ≤142m), decree-era (≥2012) events →
  zcta-level first_event_year, n_events, dose 1-4/5+.

## Data (data/experian/ — all small, committed; data/third_party/zcta_pr/ = 2020
## cartographic ZCTA polygons filtered to 132 PR ZCTAs, from cb_2020_us_zcta520_500k)
- `pr_zip_universe.csv`, `pr_zip_year_counts.csv`, `pr_ctks.csv`, `scan_summary.txt`
- `pr_consumer_histories.csv` (523,835 rows, 30MB — full panel for PR-ever consumers)
- `pr_outmigration_zip_year.csv` (3,306 zip×year cells, 231 zips with risk sets)
- `pr_outmigration_island_year.csv`, `migration_summary.txt`
- `zip_treatment.csv` — 90 treated ZCTAs (53 dose 1-4, 37 dose 5+); top: 00646
  Dorado (191 events), 00791 Palmas del Mar (146), 00907 Condado (126), 00911 (81),
  00979 Isla Verde (56). 92.1% agreement with legacy dominant-ZCTA column.
- `zip_treatment_events.csv` (event-level with joined zcta5)
- `pr_outmigration_zip_year_treated.csv` — ANALYSIS-READY: outmigration panel merged
  with treatment (all 90 treated zctas present in Experian; 295K of 393K at-risk
  person-years are in treated zips)

## Notes / caveats
- Experian zip_cd is USPS zip; treatment is on ZCTA polygons. 237 USPS zips vs
  132 ZCTAs — non-ZCTA zips are mostly PO-box zips; they appear in the migration
  panel but can't receive polygon-based treatment (they're untreated by construction —
  consider dropping non-ZCTA zips in analysis, or crosswalking).
- Migration = zip change between consecutive archive years; within-zip moves invisible;
  consumers must have a credit file (adults, credit-active — coverage caveat for PR).
- Header year is archive vintage (point-in-time address snapshot each year).

## Next steps
- User pushes commits (Rule 4).
- DiD of outmigration on zip treatment (mirror design2 staggered spec; outcomes:
  rate_move_off_pr, rate_move_any; also in-migration/composition via vantage score).
- Consider vantage_v4_score of movers vs stayers (who leaves treated zips?).
