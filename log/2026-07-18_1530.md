# Log — 2026-07-18 15:30 — Property scraping sub-project + Design 1 DiD dataset

## Rule violation acknowledged & remediated
The 2026-07-17/18 scraping sessions ran with the Dropbox folder as working directory and
never loaded this repo's CLAUDE.md — all scripts/outputs were (wrongly) written into
Dropbox. Remediation: everything copied into this repo (below); Dropbox originals left
untouched per Rule 1. Going forward: Dropbox = read-only, all writes here.

## What was built (scripts in code/python/, data in data/)

**Pipeline (CRIM = ArcGIS REST via token proxy `catastro.crimpr.net/proxy/proxy.ashx?<url>`,
layer Parcelario/Parcelas/MapServer/654; POST for long IN-lists):**
- `crim_rest_enrich.py` — re-pulled all 26,729 name-matched catastros → `data/third_party/crim_parcel_enriched.csv` (26,673 rows, coords, one sale each; CRIM stores ONLY the last sale per parcel — earlier sales are censored by resale)
- `flag_unique_matches.py` — unique_name_match flags (819 CRIM-unique names / 810 parcels)
- `crim_nearby_sales.py` — buffer scrapes (checkpoint/resume, keep-awake) → nearby_sales_{250m,1000m}_{uniquematch,all,karibe}.csv
- `add_investor_sale_cols.py`, `flag_property_age_signals.py` — annotations on all outputs
- `build_karibe_uniquematch_base.py` — Karibe unique names + catastro extracted from desc_registral → 755-parcel base (Karibe itself is now login+reCAPTCHA-walled; only old search results usable)
- `build_high_confidence_universe.py` — tiered universe: crim_unique + karibe_unique + validated multi-match recovery = 1,543 names (26.7% of 5,770) / 1,874 parcels → `data/cleaned/investor_parcels_high_confidence.csv`
- `build_uniquematch_tracts.py` + TIGER spatial joins → tract (+dominant ZCTA) on
  `crim_parcels_uniquematch_tracts.csv`, `combined_parcels_uniquematch_tracts.csv` (1,306 parcels), `pr_tract_zcta_crosswalk.csv`

**Design 1 (stacked ring event-study; user's DiD):**
- `build_design1_event_panel.py` → `data/design1/design1_events.csv` (1,256 dated events)
  + `design1_sale_event_pairs.csv` (3.47M pairs, 673MB, GITIGNORED — regenerate by running the script)
- Rings: 0-250 treat / 250-400 gap / 400-1000 control; event_time_months; flags for nominal prices, junk dates, investor-parcel sales; clean_event_1000m + n_other_events_within_1000m
- KEY FACTS: only 158/1,256 events are isolation-clean (median 25 other events within 1km — use dose or clean-at-250m); event dates reach back to 1979 (restrict to decree era in analysis); outcome deflation deliberately NOT applied (user decision: tract×time FE in estimation instead; user's tract-month price file is a MEAN price, not an index — do not deflate by it, mechanical contamination)

## Gitignored (>100MB, local-only, regenerable)
design1_sale_event_pairs.csv, nearby_sales_1000m_uniquematch_combined.csv, nearby_sales_250m_all.csv

## Addendum (16:10) — Stata starter for Design 1
`code/stata/design1_eventstudy_lpdid_csdid.do`: ±2yr event study via lpdid + csdid,
each without/with tract FE. Panel mapping: event×ring×quarter cells (near=0-250m
treated at event qtr; far=400-1000m never-treated; gap dropped); tract-FE variant
residualizes sale-level ln(price) on tract dummies (reghdfe) before collapsing.
Filters: decree-era events (≥2012), junk dates, nominal prices, investor-parcel
sales. Toggles: CLEAN_ONLY, MIN_EVYEAR. Outputs → output/design1/. Not yet run
(no Stata on this machine) — expect sparse-cell warnings; see notes in the do-file.

## Next steps
- User pushes commits (Rule 4)
- Run the Stata do-file; check cell sparsity; consider half-year aggregation or the user's tract-month price panel as alternative outcome
- Analysis in R (fixest): restrict events to ≥2012, choose clean-event definition, tract×quarter FE; municipio repeat-sales index as robustness deflator
- Open leads on match gap: LLC/entity purchases (needs PR corporate registry), 126 common names unrecoverable, decree-ID↔name crosswalk would unlock annual-report disambiguation
